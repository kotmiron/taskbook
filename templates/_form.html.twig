
{{ form_start(form, {method: 'POST', action: path('task_new')}) }}
    {{ form_errors(form) }}

                <div class="form-group">
                    {{ form_label(form.user, null, {'label_attr': {'class': 'control-label'}}) }}
                    {{ form_widget(form.user, {'attr': {'class': 'form-control'}}) }}
                    {{ form_help(form.user) }}

                </div>

                <div class="form-group" {% if not form.content.vars.valid %}has-error{% endif %}>
                    {{ form_label(form.content, null, {'label_attr': {'class': 'control-label'}}) }}

                    {{ form_errors(form.content) }}
                    {{ form_widget(form.content, {attr: {'class': 'form-control'}}) }}
                    {{ form_help(form.content) }}
                </div>

                <div class="form-group">
                    <button class="btn btn-primary pull-right" type="submit">
                        <i class="fa fa-calendar-plus" aria-hidden="true"></i> Submit
                    </button>
                </div>
{{ form_end(form) }}
